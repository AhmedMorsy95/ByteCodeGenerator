int x;
int y;
x = 100;
y = 50;
int z;
z = 51;

boolean f;
f = !true;

System.out.println(f);
// I <3 Indomie Sha3reya

if (!f && (x == 100) && (x < y + z) && !(y > z))
{
    x = 2;
}
else
{
    x = x + 3;
}

// حادي بادي كرمب زبادي
// إيه اللي جاب الكرمب على الزبادي؟
// مين المحشش اللي ألف الأغنية دي؟
// I don't care, it can be useful during MCQ questions :D
// ^ This was to test the comments recognition in our flex/bison code.

System.out.println(x);
