int x;
int y;
x = 100;
y = 50;
int z;
z = 51;

boolean f;
f = (x == 100) && y == 50;

System.out.println(f);

if (f && (x < y + z) && !(y > z))
{
    x = 2;
}
else
{
    x = x + 3;
}

// This is to test the comments recognition in our flex/bison code.

System.out.println(x);
